version: 2

models:
  - name: rule_user_inactive
    description: "ACCOUNT_USAGE view allows to get detailed info for users and their activity in the Snowflake account."
    meta:
      title: "Remove long time inactive users"
      category: "User"
      doc_section: "Account"
    tests:
      - is_empty
