version: 2

models:
  - name: rule_user_password_not_changed
    description: "As a best practice, users should change their Snowflake password regularly. This rule allows to identify users who haven't changed their password for a specified time period (e.g. 180 days)."
    meta:
      title: "Change password regularly"
      category: "User"
      doc_section: "Security"
    tests:
      - is_empty
